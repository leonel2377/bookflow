{
  "name": "bookflow-mobile",
  "version": "1.0.0",
  "private": true,
  "description": "Coque native Capacitor pour BOOKFLOW (App Store + Play Store)",
  "scripts": {
    "sync": "cap sync",
    "android": "cap open android",
    "ios": "cap open ios",
    "build:android": "cap sync android && cap open android",
    "build:ios": "cap sync ios && cap open ios"
  },
  "dependencies": {
    "@capacitor/android": "^7.2.0",
    "@capacitor/core": "^7.2.0",
    "@capacitor/ios": "^7.2.0",
    "@capacitor/splash-screen": "^7.0.1",
    "@capacitor/status-bar": "^7.0.1"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.2.0",
    "typescript": "^5.8.3"
  },
  "capacitor": {
    "platforms": [
      "android",
      "ios"
    ]
  }
}
